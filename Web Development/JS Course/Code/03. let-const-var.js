var country = "India";
var continent = "Asia"
var population = "130 million"


/*
Set the value of language to the language spoken where you live (some countries have multiple languages, but just choose one).

Think about which variables should be const variables (which values will never change, and which might change?). Then, change these variables to const.

Try to change one of the changed variables now, and observe what happens.v
*/

let isIsland = false;
let language;

language = "gujarati"

console.log(language);

// the values of country, continent and isIsland are permanent in nature and cannot be changed while other variables can be changed


// Gives error
// const isIsland;